#!/usr/bin/env bash

# MacOS
if [[ -f  /Applications/Chromium.app/Contents/MacOS/Chromium ]]; then
    chromium=/Applications/Chromium.app/Contents/MacOS/Chromium
else
    chromium="chromium-browser"
fi

exec "$chromium" --headless --disable-gpu --repl "$@"
